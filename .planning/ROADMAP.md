# Дорожная карта (Codex, этап 2)

Дедлайн < 1 суток. Каждая фаза — отдельный коммит(ы) Codex в `main`.
Порядок строгий: фаза начинается только когда тесты предыдущей зелёные.
**Порядок выполнения: 1 → 2 → 3 → 6 → ревью (`REVIEW.md`) → 7 → 8 (A–C) → переименование в Astrea → 9 (A–C) → 8D → 5 → 4 → 10 (A–E, отложено, только по команде человека).**

| # | Фаза | Результат | Таймбокс |
|---|------|-----------|----------|
| 1 | Загрузчики и единая модель данных | `app/loaders/`, `tests/fixtures/`, синтетические тесты читаются | ~3 ч |
| 2 | Доработка загрузчиков + движок | `current_stock`, NaN→0, `app/engine/`, тесты R1–R6 зелёные, `scripts/backtest.py` (Q1) | ~6 ч |
| 3 | UI и экспорт | `app/ui/streamlit_app.py`, экспорт xlsx | ~3 ч |
| 6 | ML-модель-соперник (решение человека 2026-09-23), **делается после фазы 3, до фаз 4 и 5** | `app/engine/ml.py`, переключатель метода в UI, ML в `backtest.py`; в продукт по правилу из плана фазы | ~2–3 ч |
| 7 | Корректировка и утверждение заказа, PostgreSQL, **контейнеризация** (решение человека 2026-09-23), после закрытия `REVIEW.md`, до фаз 4 и 5 | `app/orders.py`, `app/db.py`, редактируемая таблица, «История заказов»; `Dockerfile` + `docker-compose.yml` (`app` + `db`) — весь проект одной командой `docker compose up` | ~3 ч |
| 8 | Вход/регистрация по коду приглашения + редизайн, меньше текста (решение человека 2026-09-23 после живого теста) | `app/auth.py`, таблица `users`, экран входа; тема и CSS, раскладка без боковой панели, детали по клику вместо колонки «Обоснование»; UI разбит на модули | ~4–5 ч |
| 9 | Навигация слева с профилем, страницы «Заказ / Данные / История заказов / Точность», загрузка выгрузок 1С пользователем (решение человека 2026-09-23) | `st.navigation`, `app/ui/pages/`, `app/datasets.py`, таблица `datasets`, том `uploads`, страница «Точность» из бэктеста | ~3–4 ч |
| 10 | Опциональная часть RULE.md п.8 + понятный экран заказа (решение человека 2026-09-23) | по умолчанию только «требует решения»; риск дефицита и «хватит на N дн.»; страница «Поставщики» (условия); страница «Аналитика» (тренды); отправка заказа поставщику только после утверждения и подтверждения, журнал | ~3–4 ч |
| 4 | README (**ведёт Claude**, решение человека 2026-09-23) | черновик по текущему состоянию уже в README.md; Claude обновляет после фаз 2 и 3 | ~1 ч |
| 5 | Деплой на VPS (решение человека 2026-09-23), **делается последней** | `docker-compose.prod.yml` (Caddy: HTTPS + пароль), `deploy/Caddyfile`, `deploy/deploy.sh`; образ и основной compose — из фазы 7; человек — сервер и DNS по `docs/deploy.md` | ~1 ч |

Резерв ~3 ч на исправления после ревью (этап 4–5).

Планы фаз: `.planning/phases/0N-*/PLAN.md`. Текущее состояние: `.planning/STATE.md`.
