# Автозаказ: расчёт заказов поставщикам для «Электрокомплекта»

Команда Astrea · хакатон HACKALEM AI · кейс ТОО «Электрокомплект» (ekt.kz)

> **Статус на 2026-09-23.** Готовы загрузка данных и движок расчёта рекомендаций
> (фазы 1–2 из 4). Интерфейс и выгрузка в работе, см. [план](#статус-и-план).
> README описывает только то, что уже есть в репозитории, и обновляется после каждой фазы.

## 1. Название проекта

**Автозаказ** — сервис автоматического расчёта рекомендованных заказов поставщикам для пополнения склада.

## 2. Краткое описание

**Проблема.** Менеджер отдела закупа «Электрокомплекта» считает, сколько товара дозаказать у поставщиков, вручную в Excel. Это трудоёмко, поэтому расчёт делают редко и не в реальном времени. В итоге по одним позициям склад затоварен, по другим возникает дефицит. Разовые крупные продажи одному клиенту искажают оценку регулярного спроса (постановка задачи — [RULE.md](RULE.md)).

**Для кого.** Для менеджера отдела закупа. Он запускает расчёт по поставщику и получает список позиций с рекомендуемым количеством и объяснением каждой цифры, затем проверяет и утверждает заказ сам.

## 3. Что реализовано

| Возможность | Статус |
|---|---|
| Загрузка xlsx-выгрузок из 1С по двум поставщикам (IEK, Systeme Electric) с разными форматами файлов | ✅ готово |
| Приведение к единой модели: продажи по накладным, продажи и остатки по месяцам, товар в пути, кратность (MOQ), справочник SKU | ✅ готово |
| Фильтрация служебных документов (приходные накладные, заказы покупателей) и нормализация знаков количества | ✅ готово |
| MOQ = 1 для SKU, у которых кратность не указана | ✅ готово |
| Текущий свободный остаток на дату среза (22.09.2026); пустые ячейки остатков = 0 | ✅ готово |
| Сегментация спроса: регулярный / редкий / без продаж (последние идут в список «на проверку», не заказываются) | ✅ готово |
| Исключение разовых крупных продаж (робастный z-score по строкам накладных) | ✅ готово |
| Восстановление спроса в месяцы с нулевым остатком | ✅ готово |
| Прогноз: сезонность (SKU + поставщик) × рост год к году | ✅ готово |
| Заказ: потребность на срок поставки + период покрытия, страховой запас, минус остаток и товар в пути, округление до кратности; срочность | ✅ готово |
| Текстовое обоснование каждой рекомендации | ✅ готово |
| Проверка на истории против Excel-методики партнёра (`scripts/backtest.py`) | ✅ скрипт готов, итоговые цифры — после доработки метрик |
| Тесты на синтетических данных, включая все критерии must have из кейса | ✅ готово |
| Учёт категорий товаров, плановый прирост как параметр | ⏳ в работе (фаза 3) |
| Интерфейс Streamlit и выгрузка xlsx для загрузки в 1С | ⏳ в работе (фаза 3) |

## 4. Как работает решение

Целевой сценарий. Шаги 1–3 реализованы, шаг 4 в работе:

1. **Входные данные** — выгрузки из 1С в `data/raw/`: продажи, остатки, товар в пути, кратность. ✅
2. **Загрузка** — `load_all()` читает файлы обоих поставщиков и приводит их к единой модели. ✅
3. **Расчёт** — `build_recommendations()`: по каждому SKU очищенный от выбросов и восстановленный после дефицитов прогноз спроса на срок поставки плюс период покрытия, плюс страховой запас, минус остаток и товар в пути, с округлением до кратности. Каждая строка получает обоснование и срочность. ✅
4. **Результат** — таблица рекомендаций по поставщикам с обоснованием и срочностью, выгрузка в xlsx. Менеджер проверяет и утверждает сам, автоматической отправки поставщику нет. ⏳

## 5. Технологии

- **Язык:** Python 3.9+
- **Библиотеки** (версии зафиксированы в [requirements.txt](requirements.txt)): `pandas`, `numpy`, `openpyxl` (чтение xlsx), `streamlit` (интерфейс, фаза 3), `pytest` (тесты)
- **AI-модели, внешние API и сервисы:** не используются. Расчёт выполняется локально на выгрузках партнёра.

## 6. Архитектура проекта

```
data/raw/                      выгрузки 1С от партнёра (xlsx)
  ├─ IEK/
  └─ Systeme electric/
        │
        ▼
app/loaders/                   ✅ загрузка и нормализация
  ├─ iek.py                    адаптер форматов IEK
  ├─ se.py                     адаптер форматов Systeme Electric
  ├─ _shared.py                общий разбор: поиск заголовков, месяцы, «широкие» таблицы → «длинные»
  └─ __init__.py               load_all(): оба поставщика → единая модель
        │
        ▼
app/models.py                  ✅ схема единой модели и её проверка (validate_model)
        │  sales_tx · sales_monthly · stock_monthly · current_stock · in_transit · moq · sku_ref
        ▼
app/engine/                    ✅ расчёт рекомендаций
  ├─ segmentation.py           регулярный / редкий / без продаж
  ├─ cleaning.py               исключение разовых крупных продаж
  ├─ stockout.py               восстановление спроса в месяцы дефицита
  ├─ forecast.py               уровень × рост г/г × сезонность
  ├─ order.py                  окно потребности, страховой запас, кратность, срочность
  ├─ explain.py                текст обоснования
  └─ pipeline.py               build_recommendations(): всё вместе
app/config.py                  сроки поставки, период покрытия, уровень сервиса
scripts/backtest.py            проверка на истории против методики партнёра
app/ui/                        ⏳ фаза 3 — интерфейс и выгрузка
```

Адаптеры поставщиков изолируют различия форматов (разные заголовки, число строк шапки, колонки кратности и товара в пути). Всё, что ниже `app/models.py`, работает с одной схемой и не знает, откуда пришли данные.

## 7. Установка и запуск

```bash
git clone https://github.com/BAITC-Hacks/hack-3cf19ef3-astrea.git
cd hack-3cf19ef3-astrea

python3 -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate
pip install -r requirements.txt

pytest                              # тесты
```

Данные партнёра уже лежат в репозитории (`data/raw/`), докладывать ничего не нужно.

Команда запуска интерфейса появится после фазы 3.

## 8. Как проверить решение

Сейчас можно проверить загрузку данных и расчёт рекомендаций.

**1. Тесты:**

```bash
pytest
```

Ожидаемый результат — все тесты проходят (`passed`, без `failed`).

**2. Загрузка реальных данных партнёра** (~20 секунд):

```bash
python -c "
from pathlib import Path
from app.loaders import load_all
for name, table in load_all(Path('data/raw')).items():
    print(f'{name:14} {table.shape}')
"
```

Команда выводит таблицы единой модели с числом строк и колонок, например `sales_tx` около 249 тыс. строк продаж по накладным обоих поставщиков.

**3. Расчёт рекомендаций** (~30 секунд):

```bash
python -c "
from pathlib import Path
from app.loaders import load_all
from app.engine import build_recommendations
orders, review = build_recommendations(load_all(Path('data/raw')))
print(len(orders), 'рекомендаций,', len(review), 'SKU на проверку')
for row in orders.head(3).itertuples():
    print(row.supplier, row.sku_code, row.recommended_qty, 'шт.,', row.urgency)
    print('  ', row.explanation)
"
```

Команда выводит число рекомендаций и первые три строки с обоснованием.

## 9. Данные и интеграции

Источник — выгрузки из 1С, переданные партнёром (`data/raw/`). Данных о клиентах в них нет: в продажах только номер накладной, без ID клиента.

| Данные | IEK | Systeme Electric |
|---|---|---|
| Продажи по накладным | `Динамика продаж_2025-2026.xlsx` | `Динамика продаж_Syseme Electric_2025-2026.xlsx` |
| Продажи по месяцам (01.2024–09.2026) | `Ежемесячные продажи…xlsx` | `Ежемесячные продажи…SystemElectric 2024-2026.xlsx` |
| Остатки на начало месяца | `Ежемесячные остатки…ИЭК.xlsx` | `Ежемесячные остатки SystemElectric 2024-2026.xlsx` |
| Товар в пути на 22.09.2026 | `Путь ИЭК 22.09.2026.xlsx` | `Товар в пути_SystemElectric на 22.09.2026.xlsx` |
| Кратность (MOQ) | `MOQ ИЭК.xlsx` | `MOQ SystemElectric.xlsx` |
| Сезонность по компании (в деньгах) | `Сезонность ИЭК.xlsx` | `Сезонность SystemElectric 2024-2026.xlsx` |

Один склад — Алматы. Данные заканчиваются 22.09.2026, поэтому сентябрь 2026 — неполный месяц. Файлы сезонности в деньгах лежат в репозитории, но загрузчиком не читаются.

Внешних интеграций и API нет.

## 10. Ограничения

**Текущей версии:**
- Интерфейса и выгрузки в xlsx пока нет (фаза 3). Категории товаров и плановый прирост пока не учитываются.
- Текущий остаток IEK оценён: остаток на начало сентября минус продажи с 1 по 22 сентября, без сентябрьских приходов (их нет в данных). Остаток SE — фактический снимок на 22.09.
- Загрузка всех файлов занимает ~20 секунд, расчёт — ещё ~7 секунд.

**Данных и постановки (останутся и после фаз 2–3):**
- ID клиента в данных нет, поэтому разовые крупные заказы определяются по накладной, а не по клиенту.
- Сроков поставки в выгрузках напрямую нет. Для IEK они восстанавливаются из дат в документах «в пути», для Systeme Electric принимаются как допущение.
- Автоматической отправки заказа поставщику нет и не будет: это запрещено условиями кейса. Сервис только рекомендует, решение за менеджером.

## 11. Deployed-версия

Нет. Решение запускается локально (см. раздел 7).

---

## Статус и план

| Фаза | Содержание | Статус |
|---|---|---|
| 1 | Загрузчики и единая модель данных | ✅ готово |
| 2 | Движок расчёта и проверка на истории | ✅ готово |
| 3 | Интерфейс Streamlit, выгрузка xlsx для 1С, категории | ⏳ в работе |
| 4 | Финальный README с результатами проверки | ⏳ |

Подробный план и критерии приёмки — [.planning/](.planning/), оценка идеи — [docs/validation.md](docs/validation.md).
